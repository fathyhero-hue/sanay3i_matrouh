ANALYTICS PAGES NAMES PATCH

الغرض:
- جعل قسم "أكثر الصفحات زيارة" في لوحة الإدارة يعرض اسم الصنايعي بدل /worker/14 عند الإمكان.

ما الذي تغير؟
- api/server.js يستخرج worker id من page_path مثل /worker/14.
- يجلب بيانات الصنايعي من جدول workers.
- يرجع top_pages وفيها name = الاسم - الحرفة - المنطقة.

طريقة التركيب:
1) استبدل api/server.js داخل مشروعك بالملف الموجود داخل هذا الباتش.
2) شغل npm run dev.
3) افتح /admin ثم تبويب التحليلات.
4) اعمل Ctrl + F5.

ملاحظات:
- لو الصفحة قديمة ومسجلة بدون id واضح، ستظهر كمسار فقط.
- لو /worker/14 لا يقابل id موجود في جدول workers، ستظهر كمسار فقط.
