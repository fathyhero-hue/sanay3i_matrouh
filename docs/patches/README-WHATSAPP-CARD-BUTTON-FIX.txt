WHATSAPP CARD BUTTON FIX

هذا الباتش يصلح زر رسائل واتساب أسفل كل صنايعي بعد باتش تسريع لوحة الإدارة.

التركيب:
1) استبدل admin.html فقط داخل backend.
2) أعد تشغيل السيرفر: npm run dev
3) افتح /admin واعمل Ctrl + F5
4) جرّب زر رسائل واتساب أسفل أي صنايعي.

لا يحتاج SQL ولا تعديل server.js.
